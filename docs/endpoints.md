# API Reference

## Overview

The shiyao daemon exposes sandbox lifecycle and execution endpoints.

## Endpoints

- `POST /v1/sandboxes`
- `GET /v1/sandboxes/{id}`
- `DELETE /v1/sandboxes/{id}`
- `GET /v1/health`
